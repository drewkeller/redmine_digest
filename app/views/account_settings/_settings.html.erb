</div>
<h3><%=l(:label_digest_account_settings)%></h3>
<div class="box">
  <p><%=l(:label_digest_account_description) -%></p>
  <span class="tabular">
  <%
	if @user.digest_account.nil?
	  account = @user.build_digest_account
	  account[:active] = Setting.plugin_digest[:default_account_enabled]
	end
  -%>
  <% @user.build_digest_account unless @user.digest_account -%>
  <% form.fields_for :digest_account do |digest_form| -%>
    <p><%= digest_form.check_box :active -%></p>
  <% end -%>
  </span>
